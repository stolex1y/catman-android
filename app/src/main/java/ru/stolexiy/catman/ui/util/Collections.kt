/*
package ru.stolexiy.catman.ui.util

fun <T> List<T>.binarySearch(range: IntRange, target: T, comparator: Comparator<T>): Int {

    var l = range.first
    var r = range.last
    while (r > l) {
        val m = l + (r - l) / 2
        if (this[m] == target)
            return m
        else if ()
    }
}*/
